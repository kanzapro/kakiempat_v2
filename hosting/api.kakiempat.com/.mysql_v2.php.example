<?php
/**
 * Salin ke .mysql_v2.php di docroot api.kakiempat.com — jangan commit.
 *
 * Keamanan:
 * - host WAJIB localhost (API & MySQL satu server cPanel)
 * - user khusus kakiempa_v2_user, hak SELECT/INSERT/UPDATE/DELETE saja
 * - Lihat schema/mysql/020_db_access_hardening.sql untuk GRANT minimal
 */
return [
    'host' => 'localhost',
    'db' => 'kakiempa_v2',
    'user' => 'kakiempa_v2_user',
    'pass' => 'GANTI_PASSWORD_KUAT_MIN_16_KARAKTER',
    'port' => 3306,

    // Pooling (6 subdomain → 1 API → MySQL localhost):
    // persistent=true → reuse koneksi per PHP-FPM worker (kurangi overhead connect).
    // Pastikan max_connections MySQL >= jumlah worker FPM aktif.
    'persistent' => false,
    'connect_timeout' => 5,
];
